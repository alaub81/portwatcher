# -------- geoipupdate.env.example --------
# Beispiel-Konfiguration für den GeoIP Sidecar (MaxMind).
# Diese Datei enthält KEINE echten Zugangsdaten.
# Kopiere sie nach geoipupdate.env und trage deine echten Werte ein.
#
# Doku: https://github.com/maxmind/geoipupdate

# MaxMind Zugang (Platzhalter!)
GEOIPUPDATE_ACCOUNT_ID=YOUR_MAXMIND_ACCOUNT_ID
GEOIPUPDATE_LICENSE_KEY=example_license_key_not_real

# Welche Datenbanken geladen werden sollen
GEOIPUPDATE_EDITION_IDS=GeoLite2-City GeoLite2-ASN

# Download-Verzeichnis im Container (nicht ändern, passt zu docker-compose.yml)
GEOIPUPDATE_DB_DIR=/var/lib/GeoIP

# Aktualisierungsintervall in Stunden (z. B. 24 = 1x pro Tag)
GEOIPUPDATE_FREQUENCY=24

# Optional: Proxy (falls benötigt)
# HTTP_PROXY=http://user:pass@proxyhost:3128
# HTTPS_PROXY=http://user:pass@proxyhost:3128

# Timezone Configuration
TZ=Europe/Berlin
